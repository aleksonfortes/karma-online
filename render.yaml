services:
  - name: karma-landing
    type: web
    env: static
    buildCommand: ./build.sh && echo "Using landing-page outputs"
    staticPublishPath: landing-page/dist
    domains:
      - karmaonline.io
    envVars:
      - key: NODE_ENV
        value: production
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    
  - name: karma-client
    type: web
    env: static
    buildCommand: ./build.sh && echo "Using client outputs"
    staticPublishPath: dist
    domains:
      - play.karmaonline.io
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://api.karmaonline.io
      - key: VITE_SOCKET_URL
        value: wss://api.karmaonline.io
      - key: VITE_ENV
        value: production
      - key: VITE_API_KEY
        sync: false
      - key: VITE_ANALYTICS_ID
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
  
  - name: karma-server
    type: web
    env: node
    buildCommand: echo "registry=https://registry.npmjs.org/\nalways-auth=false" > .npmrc && npm install --no-fund --no-audit
    startCommand: npm run start
    domains:
      - api.karmaonline.io
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: CORS_ORIGIN
        value: https://play.karmaonline.io
      - key: TRUST_CLOUDFLARE_PROXY
        value: "true"
      - key: API_KEY
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false 